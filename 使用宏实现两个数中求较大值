#define _CRT_SECURE_NO_WARNINGS 1

#define Max(a,b) a>b?a:b

#include<stdio.h>

int main()
{
	int a = 0;
	int b = 0;
	printf("请输入两个数：\n", a, b);
	scanf("%d %d", &a, &b);
	printf("较大值为：%d\n", Max(a, b));

	return 0;
}
